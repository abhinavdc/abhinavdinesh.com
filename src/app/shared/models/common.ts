export interface Project {
    name: string,
    stacks: string[],
    githubLink: string,
    demoLink: string,
    description: string,
    icon: string,
    images: string[],
    shortDescription: string
}